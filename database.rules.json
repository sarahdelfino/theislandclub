{
  "rules": {
  "events": {
    ".read": "true",
  "$uid": {
  ".write": "newData.exists()",
    ".read": "true"
  }
  },
    "members": {
      ".read": "false",
        "$uid": {
          ".write": "newData.exists()"
        }
    },
      "submittedEvents": {
        ".read": "false",
          "$uid": {
            ".write": "newData.exists()"
          }
      }
  }
 }